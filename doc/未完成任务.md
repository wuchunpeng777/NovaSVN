# NovaSVN 未完成任务

更新时间：2026-07-12

本文档记录暂停开发时仍未完成的工作，作为后续恢复入口。完整阶段规划仍以 `doc/开发计划.md` 为准。

## 固定约束

- 不实现 Git 式暂存区；文件勾选只表示当前一次提交的目标，不持久化。
- 界面和主要交互以 Versions 的 Working Copy、Timeline、Repository 为基线。
- SVN 操作必须执行真实命令，不能只修改前端状态。
- 每完成一个可独立验收的小功能，必须验证并立即单独提交 Git。
- 中文沟通，代码注释使用中文。

## 当前基线

- 分支：`main`。
- Delete 已覆盖文件、目录、空版本化目录、ignored/external 排除、工作副本归属、符号链接、排队期间目标身份变化和 Unix 特殊路径。
- 普通 SVN 操作已按 pending task id 消费完成状态，不再依赖当前选中的任务。
- Commit、Partial Commit、Repository、Checkout、Switch、Revision Diff 和 Merge 也已按各自 pending task id 消费完整任务结果。
- 打开工作副本、状态扫描和文件树刷新已有请求世代隔离，旧请求不会覆盖后来打开的工作副本。
- 工作副本根比较覆盖 Windows 盘符、UNC、根目录和 Unix 字面反斜杠。
- 后端重启或任务队列重置导致 pending task 消失时，会解除操作锁定并显示可恢复错误。
- 文件树的递归 `svn info` 已改为流式解析，版本化路径和错误输出都有明确内存边界。
- 工作副本文件和目录支持安全 Move，包含源/目标确认、身份复检和完成刷新。
- 工作副本文件和目录支持安全 Copy，源保持不变，目标作为带历史的新增项进入提交目标。
- 未版本控制文件和目录支持 Ignore；规则按直接父目录写入并保留已有 `svn:ignore` 内容，属性面板显示实际作用目录。
- 工作副本工具栏已改用 Lucide 图标按钮，并按具体操作显示 tooltip、禁用态和运行态。
- Versions 布局已统一侧栏、主表、检查器与分隔尺寸，并在 960px Tauri 最小宽度下保留主表空间。
- 项目侧栏和工作副本检查器支持独立显隐，偏好写入应用设置并在重启后恢复。
- 工作台支持浅色、深色和跟随系统主题，Monaco Diff 与状态颜色同步适配。
- 工作副本表格显示真实 Base/Last revision、最后修改日期和作者，元数据保持流式解析与池化内存边界。
- 工作副本状态区分本地、远端和同时变化，并在远端扫描回退时明确显示未检查状态。
- 文件行按状态提供 Commit、Update、Add、Resolve，文件级 Update 走真实任务，次要操作使用可访问菜单。
- 工作副本文件表支持复选框和 Shift 范围多选，可批量切换提交目标，并以单个真实任务执行 Revert、Move 或 Delete。
- Repository 文件支持按同一历史 Revision 打开临时副本，并查看 Log、Blame 和只读 Properties。
- Repository 支持按 URL 与 Revision Checkout 到本地，成功后打开工作副本。
- Repository 支持按 URL 与 Revision Export 到本地空目录，成功后在文件管理器中显示导出结果且不创建工作副本。
- Repository 支持要求提交信息与确认后创建远端目录，成功后刷新原父目录。
- Repository 支持选择本地文件或目录执行真实 Import，入队与执行前复检源路径，成功后刷新目标父目录。
- Repository 支持普通文件和目录的远端 Copy，历史 Revision 使用 peg revision 定位，成功后刷新目标父目录。
- Repository 支持普通文件和目录的远端 Move，目标父目录不存在时明确失败，成功后刷新受影响目录。

## 当前开发断点（2026-07-12）

状态：**Repository 通用 Move 已完成并可验收。** 下一个建议项是 Repository Rename，之后继续 Delete。

- Checkout：真实 `svn checkout`，成功后打开工作副本。
- Export：真实 `svn export`（无 `.svn`），成功后在系统文件管理器中显示导出路径，不打开为工作副本。
- 创建目录：真实 `svn mkdir`，要求提交信息和确认，成功后按独立 pending task id 刷新原父目录。
- Import：真实 `svn import`，支持本地文件或目录，要求提交信息和确认，执行前复检源路径。
- Copy：真实 `svn copy`，支持普通条目和历史 Revision，要求提交信息和确认。
- Move：真实 `svn move`，要求目标父目录存在、提交信息和确认。
- 验证：组件/App/Store 测试、Rust 历史目录真实 Checkout/Export、`npm run test:scripts`、`npm run build` 应通过。

## 最近完成

### 1. 修复 SVN 操作完成监听

状态：**已完成。** 只处理 `SvnOperationKind` 对应的一组操作，不代表所有任务完成处理都已解耦 `selectedTask`。

- [x] 按 `pendingSvnOperationTaskId` 从 `taskStore.snapshot.tasks` 查找终态，不再读取当前选中的任务。
- [x] 成功 Update 重载工作副本，其他成功 SVN 操作刷新状态；失败或取消只清理 pending。
- [x] pending 同时绑定操作类型和创建任务时的工作副本根，Update 使用显式根路径，不读取后来编辑的 `pathInput`。
- [x] 操作创建前增加同步 single-flight 和 pending guard，避免连续点击覆盖单一 pending id。
- [x] `openPath` 保留运行中的 pending，切换工作副本后仍禁止创建第二个普通 SVN 操作。
- [x] 覆盖 Windows 单反斜杠、盘符大小写、UNC、根目录和 Unix 特殊路径归一化。
- [x] `openPath`、状态扫描、状态分页和文件树刷新增加请求世代校验。
- [x] 增加 App、helper 和 Store 测试，覆盖连续点击、切换选中任务、切换工作副本、路径输入变化、成功/失败/取消和重复消费。
- [x] pending id 经后端确认永久缺失时清理锁定并显示可恢复错误。

### 2. Delete 发布加固

- [ ] 在 Windows 实机验证 junction/reparse point、短路径别名、尾随点/空格和 ADS 拒绝行为。
- [ ] 在 macOS 与 Windows Tauri 应用内手工验证确认弹窗、文件/目录删除、任务日志和刷新结果。
- [ ] 增加 App 层测试，锁定用户取消 Delete 确认时不会创建任务。
- [ ] 评估执行前身份复检到启动 `svn delete` 之间的极短竞争窗口；SVN CLI 路径接口目前无法原子封闭。
- [ ] 处理版本控制目录名为 `target`、`dist`、`node_modules` 等时被文件树隐藏、无法直接 Delete 的问题。

### 3. 大型工作副本性能

- [x] 文件树刷新流式解析 `svn info --xml --depth infinity`，不再缓冲完整 XML 或构建 DOM。
- [x] 版本化路径限制为 100 万条，单路径限制为 32 KiB，stderr 限制为 64 KiB 并标记截断。
- [x] 将递归 `svn info` 加入性能基准，并通过 ignored、external、空目录的既有真实工作副本测试。
- [ ] 增加稀疏工作副本回归测试，并实际执行 50000 文件、5000 改动的完整基准。

### 4. Ignore 管理

- [x] 文件树和检查器只对未版本控制文件或目录显示 Ignore。
- [x] 执行前确认目标和父目录作用域，真实执行 `svn propset` 并保留已有规则。
- [x] 拒绝重复 Ignore、版本控制目标、非版本控制父目录和其他工作副本，使用真实临时仓库覆盖成功与错误边界。
- [x] 成功后刷新状态、文件树和提交目标，并在属性面板保留父目录 `svn:ignore` 编辑值。

### 5. Versions 图标工具栏

- [x] Refresh、Update、Apply Patch 和 Cleanup 使用固定尺寸图标按钮。
- [x] 图标按钮提供无障碍名称、tooltip、键盘焦点和稳定的禁用状态。
- [x] 状态刷新、Update、Cleanup 和 Patch 运行时显示具体操作名称与旋转进度图标。

### 6. Versions 布局尺寸

- [x] CSS 最小宽度与 Tauri `minWidth: 960` 保持一致，并加入脚本门禁。
- [x] 标题栏、位置栏、侧栏和检查器分隔使用稳定尺寸与统一颜色。
- [x] 检查器限制为 300–720px，并随窗口收窄以保证主表至少保留 360px。
- [x] 检查器宽度支持鼠标拖拽、方向键和可访问的数值状态。

### 7. Versions 面板显隐

- [x] 标题栏提供项目侧栏和检查器图标开关，并显示当前动作 tooltip。
- [x] 隐藏面板时同步移除分隔器并让主内容扩展为单列。
- [x] 检查器开关只在工作副本视图启用，切换视图和重启应用后保留偏好。

### 8. Versions 主题

- [x] 主题模式支持跟随系统、浅色和深色，并持久化用户选择。
- [x] 深色主题覆盖主表、侧栏、检查器、控件、状态色、错误提示和 Patch 对话框。
- [x] 监听系统主题变化，并同步切换 Monaco Diff 的 `vs` / `vs-dark` 主题。
- [x] 设置页提供主题分段控件及侧栏、检查器可见性复选框。

### 9. Versions 工作副本元数据栏位

- [x] 流式 `svn info` 提取 Base revision、最后提交 revision、作者和日期。
- [x] 作者与日期使用去重字符串池，路径、单字段和池总量均有明确内存上限。
- [x] 文件树 API 和表格展示 Name、Base、Last、Date、Author、Status 与 Size。
- [x] 未版本控制、ignored 和 external 路径不伪造 SVN 元数据。

### 10. Versions 本地与远端状态

- [x] 解析 `wc-status`、`repos-status` 和仓库 `against` revision，不混用本地与远端状态。
- [x] 文件与目录使用 local、remote、both、none 明确表达独立及组合状态。
- [x] 表格、摘要、检查器和筛选器分别展示本地改动与远端更新。
- [x] 仅远端变化不会出现提交选择或 Revert，远端检查回退时显示“远端未检查”。
- [x] 使用两个真实工作副本验证仅远端变化和本地、远端同时变化的 SVN XML。

### 11. Versions 状态化文件操作

- [x] 本地可提交项显示 Commit，未版本控制项显示 Add，冲突项显示 Resolve，远端变化显示 Update。
- [x] 文件级 Update 校验工作副本内相对路径并执行真实 `svn update <path>`；同时有本地改动时要求确认风险。
- [x] 文件行使用名称按钮、真实操作按钮和可访问菜单，不再嵌套 `role="button"`。
- [x] Revert、Ignore、Move、Copy、Delete 收入更多菜单，运行任务期间保持稳定禁用状态。
- [x] 增加 App、组件、任务路径和真实双工作副本文件级 Update 测试。

### 12. Versions 工作副本多选

- [x] 文件表提供表头全选、行复选框、Shift 范围选择、部分选择状态和工作副本切换清理。
- [x] 所选可提交路径可一次加入或移出当前 Commit 目标，不持久化为暂存区。
- [x] 批量 Revert、Move 和 Delete 使用单个后端任务，并在执行前显示路径数量、清单和影响范围。
- [x] 后端限制批量路径数量与总长度，折叠父子重复目标，并在破坏性命令启动前复检全部身份快照。
- [x] 使用组件、App、Store、Rust 编译测试和真实临时 SVN 仓库覆盖批量操作。

### 13. Versions 文件表键盘导航

- [x] 文件表使用可聚焦 treegrid 和活动行，行、列、层级、展开与多选状态提供明确无障碍语义。
- [x] 支持 ArrowUp/ArrowDown、Home/End，以及 ArrowLeft/ArrowRight 的收起、展开、父级和子级导航。
- [x] Enter 激活文件或切换目录，Space 切换活动行选择，Ctrl/Cmd+A 选择当前可见路径。
- [x] Shift 加方向键使用稳定锚点扩展或收缩范围，不清除范围外既有选择。
- [x] 修复折叠集合、行选择和 Commit 目标被辅助函数隐藏后未触发 Svelte 模板更新的问题。

### 14. Versions 文件双击打开

- [x] 双击存在的工作副本文件时使用系统默认应用打开，目录双击保持树形展开/收起语义。
- [x] 缺失或已删除文件不启动打开操作，检查器中的打开和显示位置入口同步禁用。
- [x] 后端要求目标真实存在且为文件，并在解析符号链接后仍位于规范化工作副本根目录内。
- [x] Windows 默认应用入口直接调用 Explorer，不再经过 `cmd /C start` 解释文件名。
- [x] 增加组件、App、Rust 路径边界和静态门禁覆盖。

### 15. Versions 路径右键菜单

- [x] 右击未选路径时切换为单项选择，右击已选路径时保留当前多选集合。
- [x] 按路径状态提供 Open、Show in Folder、Commit、Update、Add、Resolve、Revert、Move、Copy、Ignore 和 Delete。
- [x] Revert、Move、Delete 根据当前兼容目标自动使用单项或批量任务入口。
- [x] 菜单限制在窗口可见区域内，并在外部点击、窗口失焦、尺寸变化或路径消失时关闭。
- [x] 支持 Menu 键、Shift+F10、ArrowUp/ArrowDown、Home/End 和 Escape，并把焦点恢复到 treegrid。

### 16. Versions 检查器标签

- [x] 使用 Information、Properties、Diff、Blame、Commit 和 Tasks 标签替代纵向堆叠面板。
- [x] Information 展示路径、状态、类型、Base、Last、Author、Date 和 Lock，并保留状态化文件操作。
- [x] Properties、Diff、Blame 保留已有真实 SVN 数据、Monaco Diff、Hunk 和属性编辑工作流。
- [x] 标签使用 tablist、tab 和 tabpanel 语义，并支持 ArrowLeft/ArrowRight、Home 和 End 键盘切换。
- [x] 增加组件测试和静态门禁，覆盖标签内容隔离、焦点移动与窄窗口布局约束。

### 17. 应用原生当前路径菜单

- [x] treegrid 将当前活动文件或目录同步给 App，并在筛选、键盘导航、鼠标激活和工作副本切换后更新。
- [x] 原生工作副本菜单增加当前路径子菜单和活动名称，提供 Open、Show in Folder、Commit、Update、Add、Resolve、Revert、Move、Copy、Ignore 和 Delete。
- [x] 根据本地/远端状态、版本控制身份、冲突、提交目标和任务锁定状态动态更新菜单文本与禁用状态。
- [x] 菜单点击复用现有真实文件入口、提交目标和 SVN 任务，执行前再次校验最新状态，拒绝过期菜单事件。
- [x] 增加状态推导、命令分派、活动行上报、Rust 编译和静态门禁覆盖。

### 18. Timeline 日期分组

- [x] 对过滤后的 revision 按本地日历日期稳定分组，组内保留 SVN 日志顺序。
- [x] 日期组显示日期和 revision 数量，滚动时组标题保持可见。
- [x] 无效或缺失日期进入“日期未知”组，不丢弃历史记录。
- [x] 选择组内 revision 后继续更新右侧改变路径和比较面板。
- [x] 增加多日、同日、未知日期、选择联动、深色主题和静态门禁覆盖。

### 19. Timeline revision 详情

- [x] 每条 revision 固定展示 revision、作者、精确到秒的时间、提交信息和改变路径数量。
- [x] 改变路径显示 action、完整仓库路径和条目类型，不依赖先选中右侧详情。
- [x] 超过 3 条路径时提供展开与收起，展开后显示全部路径且不改变 revision 选择状态。
- [x] 长路径允许换行，作者和计数保持稳定列宽，并覆盖浅色、深色主题。
- [x] 修复 Set 状态隐藏在辅助函数中导致路径展开不重新渲染的问题，并增加回归测试。

### 20. Timeline 路径 Revision Diff

- [x] Timeline 摘要和右侧改变路径列表都提供可访问的 Revision Diff 按钮。
- [x] 点击路径自动准备前一 revision 与当前 revision，并立即创建真实 Revision Diff 任务。
- [x] 使用仓库根和 SVN changed path 构造逐段编码的目标 URL，拒绝控制字符、反斜杠和 `.`/`..` 段。
- [x] Rust Revisions diff 载荷支持可选目标 URL，并把该 URL 传给 `svn diff`，其他比较模式不复用旧目标。
- [x] 增加组件、Store、任务载荷、Rust 编译和真实临时 SVN 仓库验证，确认结果不包含未点击文件。

### 21. Timeline 日期过滤

- [x] Timeline 过滤栏增加可访问的开始与结束日期控件，并用 min/max 约束互相提示。
- [x] 开始日期从本地 00:00:00 起生效，结束日期使用下一日 00:00 排他边界，完整包含结束日。
- [x] 日期过滤启用时排除无法解析日期的旧记录；开始晚于结束时显示错误并返回空结果。
- [x] 区分“尚未读取日志”和“没有符合过滤条件”，并提供一键清除关键字、作者、日期和文件过滤。
- [x] 增加当天末尾毫秒、跨日、未知日期、无效范围、清除过滤和深色主题测试。

### 22. Timeline 组合过滤与分页

- [x] 作者、关键字、日期、当前文件和每页数量控件可组合使用，过滤栏显示结果数、已加载数和更多分页状态。
- [x] 当前文件过滤显示活动文件名，无选择时禁用；切换范围立即清空旧快照并重新读取正确日志目标。
- [x] 连续分页按 `next_start_revision` 请求，合并时去除重复 revision，同时保留全部客户端过滤状态。
- [x] 区分顶栏“更多”和“加载更多 Revision”的无障碍名称，文件范围切换期间禁用重复操作。
- [x] 增加组件组合过滤、Store 范围失效、分页去重、过滤持久和静态门禁覆盖。

### 23. Timeline 双 Revision 比较

- [x] 每条 Timeline revision 提供独立比较复选框，最多选择两条，不与详情按钮嵌套。
- [x] 比较工具栏按旧到新显示 revision 范围，并提供执行和清除入口。
- [x] 选择第三条前必须先取消已有选择；分页追加保留选择，日志刷新会清理已不存在的 revision。
- [x] 执行前重新准备无目标 URL 的 revision 范围，并复用真实 Revision Diff 任务。
- [x] 增加组件、Store、浅色/深色样式和静态门禁覆盖。

### 24. Timeline 文件 Revision 与工作副本比较

- [x] Timeline 每条 revision 提供文件比较图标，只在当前选择版本化文件时启用。
- [x] 工作副本比较模式自动带入当前文件，并保留原有整工作副本比较能力。
- [x] Revision Diff 请求增加可选文件路径，后端拒绝绝对路径、父目录、控制字符和 `.svn` 路径。
- [x] 后端对文件目标执行 `svn diff -r <revision> <working-copy-file>`，不混入其他工作副本改动。
- [x] 增加组件、Store、任务载荷、Rust 校验和静态门禁覆盖，并使用真实临时 SVN 仓库验证文件范围。

### 25. Timeline Revert 到 Revision

- [x] Timeline 每条 revision 提供独立 Revert 图标，并在执行前说明反向 Merge 和本地改动语义。
- [x] 新增独立 Revert Revision 后端任务，不使用 `svn update -r` 改变工作副本基线。
- [x] 执行时重新检查本地改动、工作副本根 Revision、仓库 HEAD 和目标 Revision 范围。
- [x] 只允许干净、已 Update 到 HEAD 的工作副本回退到更早 Revision，并返回稳定可恢复错误。
- [x] 执行 `svn merge --ignore-ancestry -r <base>:<target> <url> <working-copy>`，完成后刷新状态以显示生成的本地改动。
- [x] 增加组件、App、Store、Rust 校验、真实临时 SVN 仓库测试和静态门禁覆盖。

### 26. Revision Diff 完整 Patch 导出

- [x] Revision Diff 非空结果在后端写入完整 Patch，界面预览仍使用明确字节上限。
- [x] 未截断结果可直接导出完整 Patch，截断结果提供“显示完整 Patch 位置”入口。
- [x] 截断提示直接显示完整文件名和绝对路径，长路径可换行且支持浅色、深色主题。
- [x] 截断结果缺少文件位置时显示错误并禁用无效入口；完整 Patch 写入失败时任务直接失败。
- [x] 增加组件、Store、Rust 完整文件写入和静态门禁覆盖。

### 27. Repository 指定 Revision 浏览

- [x] Repository URL 栏增加独立 Revision 输入，空值显示并请求 HEAD。
- [x] Revision 只接受单个数字版本号，后端拒绝范围、负数、关键字和控制字符。
- [x] 面包屑、父目录和目录下钻保持同一 Revision，不混用 HEAD 与历史快照。
- [x] 后端执行 `svn list --xml -r <revision> <url>`，并在结果中返回当前快照 Revision。
- [x] 增加组件、Store、Rust 解析、真实临时 SVN 仓库和静态门禁覆盖。

### 28. Repository 条目元数据

- [x] 仓库目录表使用 Name、Type、Last Revision、Author 和 Date 的稳定列结构。
- [x] Last Revision、作者和日期来自 `svn list --xml` 的条目 commit 元数据，不使用工作副本数据代替。
- [x] `dir` 和 `file` 显示为目录和文件，未知类型原样显示而不误标为文件。
- [x] 作者和日期保留完整原始值作为 tooltip，表格中使用紧凑日期格式。
- [x] 增加组件、Rust XML 解析和静态门禁覆盖。

### 29. Repository 文件临时打开

- [x] 文件行与目录行分流；目录保持导航，文件按当前历史 Revision 创建独立下载任务。
- [x] 后端执行真实 `svn cat -r <revision> <url>`，stdout 直接流式写入文件，不把二进制内容放入 IPC。
- [x] 临时文件位于应用数据目录的 `repository-files` 子目录，打开前再次校验规范化路径和目录边界。
- [x] 临时文件名过滤不安全字符并保留扩展名，完成后使用系统默认应用打开。
- [x] 任务按独立 pending id 消费，覆盖成功打开、下载失败、打开失败和重复操作禁用状态。
- [x] 增加组件、App、Store、Rust 编译测试和静态门禁；真实 SVN 测试覆盖二进制历史 Revision 与失败文件清理。

### 30. Repository 文件 Log

- [x] 文件行提供独立 Log 图标，不改变文件名点击打开临时副本和目录导航行为。
- [x] 后端按当前 Repository 历史 Revision 执行真实 `svn log --xml --verbose --limit`，并附加正确 peg revision。
- [x] Log 首屏从当前快照向旧 Revision 查询，连续分页不能越过快照且合并时去除重复 Revision。
- [x] Repository Log 使用独立请求世代和 loading/error 状态，关闭面板或切换目录后旧响应不会回填。
- [x] 面板显示目标 URL、快照 Revision、作者、时间和提交信息，并支持加载更多与关闭。
- [x] 增加组件、App、Store、Rust 编译测试和静态门禁；真实临时 SVN 仓库覆盖 HEAD 已删除文件的历史 Log 和分页。

### 31. Repository 文件 Blame

- [x] 文件行提供独立 Blame 图标，与临时打开和 Log 操作分流，并为图标提供明确 tooltip 和无障碍名称。
- [x] 后端在当前历史快照执行真实 `svn blame --xml -r 1:<revision>`，并用同一 peg revision 支持 HEAD 已删除文件。
- [x] 文件正文通过同一快照的 `svn cat -r <revision>` 读取，非 UTF-8 内容返回稳定错误而不进入 IPC 逐行结果。
- [x] Blame 面板显示目标 URL、快照 Revision、总行数、截断状态以及逐行 Revision、作者、行号和内容。
- [x] Repository Log 与 Blame 请求世代互相隔离，切换视图后旧响应不会覆盖当前面板。
- [x] 增加组件、App、Store、Rust 编译测试和静态门禁；真实临时 SVN 仓库覆盖历史删除文件的两次逐行归属与截断。

### 32. Repository 文件 Properties

- [x] 文件行提供独立 Properties 图标，与临时打开、Log 和 Blame 操作分流，并提供明确 tooltip 和无障碍名称。
- [x] 后端在当前历史快照执行真实 `svn proplist --xml --verbose -r <revision> URL@<revision>`，支持 HEAD 已删除文件。
- [x] 属性按名称排序并保留多行值；SVN 以 base64 返回的二进制属性使用明确标记只读展示。
- [x] Log、Blame 和 Properties 使用独立请求世代并互斥显示，切换视图后旧响应不会覆盖当前面板。
- [x] 增加组件、App、Store、Rust 编译测试和静态门禁；真实临时 SVN 仓库覆盖两个历史版本、HEAD 删除和二进制属性。

### 33. Repository Checkout

- [x] 后端独立 Checkout 任务执行真实 `svn checkout`，并组合 operative/peg revision。
- [x] 入队与执行前复检本地目标：不存在或为空；拒绝非空目录、符号链接和无效父目录。
- [x] Repository 视图提供 Checkout 表单、当前 URL 填充和父目录选择。
- [x] 按独立 pending task id 消费完成状态；成功后打开本地工作副本并切换到改动视图。
- [x] 增加 Store、App、组件、Rust 校验、真实临时 SVN 历史目录 Checkout 和静态门禁覆盖。

### 34. Repository Export

- [x] 后端独立 Export 任务执行真实 `svn export`，并组合 operative/peg revision。
- [x] 入队与执行前复检本地目标：不存在或为空；导出结果不含 `.svn` 元数据。
- [x] Repository 视图提供 Export 表单、当前 URL 填充和父目录选择。
- [x] 按独立 pending task id 消费完成状态；成功后打开本地路径位置，不打开为工作副本。
- [x] 增加 Store、App、组件、Rust 校验、真实临时 SVN 历史目录 Export 和静态门禁覆盖。

## 待开发

### P0：任务与命令基础设施

- [x] 将 Commit、Partial Commit、Repository List/Copy、Checkout、Switch、Revision Diff、Merge 等其余完成处理逐项改为按 pending task id 查找，不依赖 `selectedTask`。
- [x] 为任务完成处理增加成功、失败、取消、切换选中任务和重复消费测试。
- [ ] 应用重启后识别未完成、中断或已经从任务快照消失的任务，并给出可恢复状态。
- [ ] 运行中任务可真正终止子进程，而不是只更新前端状态。
- [ ] 保存 SVN 子进程句柄，取消时终止进程树，并增加命令超时和无输出超时。
- [ ] 统一任务日志中的命令、退出码和取消原因，确保凭据不会进入日志或诊断包。
- [ ] 为 SVN 命令统一非交互、用户名/密码/SSH、系统凭据、证书信任和错误恢复策略。
- [ ] 修复状态第 501 条之后的文件无法进入常用操作流程。
- [ ] 统一限制命令输出、任务日志和结果文本大小；Apply Patch 极端大输出目前仍可能放大内存与 IPC 数据量。

### P1：提交模型与工作副本操作

- [ ] 为当前主界面的提交目标勾选补组件测试，确认切换工作副本或任务不会恢复旧选择。
- [ ] 确保提交表单在 Commit 命令后立即可见并获得焦点，安全检查只约束本次勾选目标。
- [ ] 覆盖 Commit 成功、失败和取消后的 pending 清理与提交目标刷新。
- [ ] 为 Add 的 ignored、缺失和工作副本外路径提供稳定错误与回归测试。
- [x] 实现工作副本内 `svn move`，包含源/目标校验、确认、真实仓库测试和刷新。
- [x] 实现工作副本内 `svn copy`，包含源/目标校验、确认、真实仓库测试和刷新。
- [x] 实现 Ignore 管理，覆盖 `svn:ignore` 与按目录作用域展示。
- [x] 增加多选 Revert、Delete、Move 等批量入口，并明确影响范围。
- [x] 补齐工作副本路径的状态化右键菜单。
- [x] 增加应用原生菜单中的当前路径操作入口和动态禁用状态。

### P1：Versions 工作副本界面

- [x] 将工具栏改为图标按钮，补齐 tooltip、禁用态和运行态。
- [x] 统一标题栏、侧栏、主表和检查器的分隔与尺寸，修复 CSS 最小宽度与 Tauri 最小窗口不一致。
- [x] 文件表格补齐 Name、Base、Last、Date、Author 等栏位。
- [x] 区分本地改动与远端更新。
- [x] 按状态提供 Commit、Update、Add、Resolve 等文件操作。
- [x] 使用真实按钮或菜单替换文件行内嵌套 `role="button"` 交互。
- [x] 增加路径多选和批量 Commit、Revert、Move、Delete。
- [x] 增加文件表键盘导航。
- [x] 增加双击打开。
- [x] 增加右键菜单。
- [x] 将检查器整理为 Information、Properties、Diff、Blame 等标签。
- [x] 增加侧栏/检查器显隐。
- [x] 支持浅色、深色和跟随系统主题。

### P1：Timeline

- [x] 按日期分组显示 revision。
- [x] 完整展示作者、时间、提交信息和改变路径。
- [x] 改变路径可打开 revision diff。
- [x] 增加日期起止过滤控件。
- [x] 作者、关键字、数量和文件历史过滤可与日期组合使用并连续分页。
- [x] 支持比较任意两个 revision。
- [x] 支持比较文件任意 revision 与工作副本。
- [x] 支持回退到指定 revision。
- [x] 导出完整 Patch；截断时明确给出产物位置。

### P1：Repository

- [x] 支持浏览任意 URL 和指定历史 revision。
- [x] 展示 Last revision、日期、作者和条目类型。
- [x] Repository 文件支持临时副本打开、Log、Blame 和 Properties。
- [x] 支持 Checkout。
- [x] 支持 Export。
- [ ] 补齐创建目录、Import、Copy、Move、Rename 和 Delete。
- [ ] 支持拖入文件或目录执行 Import、拖出条目执行 Export。
- [ ] 为仓库写操作统一提交信息、确认、权限错误和刷新行为。

### P1：Patch 与 Merge

- [ ] Apply Patch 增加偏移应用场景，并为极端大输出统一截断任务日志和 IPC 预览。
- [ ] Merge 支持单源 URL、revision 范围、dry-run、结果预览和 merge tracking 常用参数。
- [ ] Merge 执行前重新检查工作副本，不依赖旧状态；冲突结果直接进入 Resolve 工作流。

### P2：测试、性能与发布

- [ ] 恢复当前 `MainWorkspace` 组件测试，覆盖提交目标和常用行操作。
- [ ] 增加主工作台端到端测试，覆盖打开工作副本、Add、Delete、Commit、Revert、Update、Merge 和 Patch。
- [ ] 更新 Playwright 冒烟测试并安装浏览器依赖。
- [ ] 扩充真实临时 SVN 仓库测试，覆盖 Add、Commit、Revert、Update、Merge 和 Patch。
- [ ] 提供跨平台统一的 `npm run check`，修复 Clippy `-D warnings`，并把静态脚本门禁纳入 CI。
- [ ] 建立 50000 文件、5000 改动的性能基准，覆盖状态分页、文件树、Diff、Blame 和 Timeline 的非冻结行为。
- [ ] 在 Windows 实机验证 NSIS、Explorer 入口、路径别名和 SVN 可执行文件发现。
- [ ] 在 macOS 实机验证 DMG、Finder Quick Actions、签名和 notarization。
- [ ] 补齐正式代码签名、发布证书、版本同步、发布产物校验和升级策略，并在 Windows/macOS 各完成一次真实远端 SVN 验收。

## 建议恢复顺序

1. 继续仓库写操作（创建目录、Import、Copy/Move/Rename/Delete）；同时补齐大型工作副本与稀疏工作副本验收。
2. 完善任务取消、超时、认证和证书流程，最后完成 Windows/macOS 发布验收。

## 当前验证环境限制

- 当前 Windows 环境可完成 `cargo test --no-run`，但 Rust 测试进程启动时报 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`，因此真实临时仓库用例只能确认编译通过。
- Playwright Chromium 尚未安装，端到端测试暂不能执行；组件测试和生产构建不受影响。

## 恢复检查

开始下一项工作前执行：

```bash
git status --short
git log -5 --oneline
npm run test:components
npm run build
npm run test:scripts
cd src-tauri
cargo test
cargo fmt --all -- --check
cd ..
git diff --check
```

开始新功能前确认工作区干净，并从上面的建议恢复顺序继续。
