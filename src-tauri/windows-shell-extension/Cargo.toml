[package]
name = "novasvn-shell-extension"
version = "0.1.0"
edition = "2021"

[lib]
name = "novasvn_shell_extension"
crate-type = ["cdylib"]

[dependencies]
windows-core = "0.61.2"
windows = { version = "0.61.3", features = [
  "Win32_Foundation",
  "Win32_System_Com",
  "Win32_UI_Shell",
] }
